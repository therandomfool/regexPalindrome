# Using regex to solve Palindrome JS challenge

- JS methods
- match(a-z0-9)+/ig to evalute regex 
- .split(), reverse(), join() || toLowerCase() to manipulate string 

# regex can be a monster to learn

![regex monster](img/regex.gif)
